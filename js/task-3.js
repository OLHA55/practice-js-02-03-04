const profile = {
  username: "Jacob",
  playTime: 300
}

//  console.log(profile);
let = getInfo() {
    return (${username } has ${ playTime } active hours!);
}
 console.log(getInfo());


// console.log(profile.Info());